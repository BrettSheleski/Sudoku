<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Sudoku.App.Mobile.MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition  Height="Auto" />
            <RowDefinition  Height="Auto" />
            <RowDefinition  Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="SudokuGrid">
            
        </Grid>

        <Button Grid.Row="1" Text="New Puzzle" Command="{Binding NewPuzzleCommand}" />
        <Button Grid.Row="2" Text="Clear" Command="{Binding ClearPuzzleCommand}" />
        <Button Grid.Row="3" Text="Solve" Command="{Binding SolveCommand}" />
    </Grid>

</ContentPage>
